class Solution:
    def duplicateZeros(self, arr: List[int]) -> None:
        """
        Do not return anything, modify arr in-place instead.
        """
        i=0;
        j=0;
        while(i<len(arr))
        if(arr[i]!=0):
            continue
            elif(arr[i]==0):
                j=0
                while(j<len(arr)):
                    arr[j+1]=arr[j]
        
               